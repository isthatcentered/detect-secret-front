/*
 * 🛑 Login
 *    - Page redirects to cloud function
 *    - Cloud function redirects to page with token
 *    - Page stores token
 * 🛑 Logout
 *    - A logout button is available when logged in
 *    - Token is cleared on logout click
 *    - User is redirected to home\log in with git page
 *    - A confirmation message is displayed
 */

test( `A user can log in`, () => {

} )

test( `A user can log out`, () => {

} )

export default undefined